// export type UserModel = {
//     id: string
//     email: string
//     businesses: {
//         id: string
//         role: string
//         createdAt: string
//     }[]
//     createdAt: string
// }

export default interface Service {
  // get(id: string): Promise<UserModel>
  // getBusinessUsers(business: string): Promise<UserModel[]>
  // inviteUser(email: string, business: string, role: string): Promise<void>
}
